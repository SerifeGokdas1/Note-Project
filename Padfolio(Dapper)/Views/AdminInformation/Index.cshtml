@model  IEnumerable<Padfolio_Dapper_.Models.AdminInformations>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPageAdmin.cshtml";
}

<div class="card">
    <div class="card-body">
        <h5 class="card-title">Admin Informations</h5>
        <!-- Table Variants -->
        <br />
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col"> &nbsp @Html.DisplayNameFor(model => model.AdminId)</th>
                    <th scope="col">@Html.DisplayNameFor(model => model.AdminNameSurname)</th>
                    <th scope="col">@Html.DisplayNameFor(model => model.AdminPassword)</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="table-primary">
                        <th scope="row">&nbsp &nbsp @Html.DisplayFor(modelItem => item.AdminId)</th>
                        <td>@Html.DisplayFor(modelItem => item.AdminNameSurname)</td>
                        <td>@Html.DisplayFor(modelItem => item.AdminPassword)</td>
                        <td>
                            <button type="button" class="btn btn-light rounded-pill"> <i class="bi bi-bookmark-plus-fill"></i>&nbsp<p>&nbsp @Html.ActionLink("Edit", "AAddUpdate", new { id = item.AdminId }) &nbsp</p></button>&nbsp&nbsp
                            <button type="button" class="btn btn-light rounded-pill"> <i class="bi bi-bookmark-x-fill"></i>&nbsp<p>&nbsp @Html.ActionLink("Delete", "ADelete", new { id = item.AdminId })&nbsp</p></button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <!-- End Table Variants -->
        <br />
        <br />
        <div class="text-center">
            <button type="button" class="btn btn-light rounded-pill"><i class="bi bi-journal-plus"><p>@Html.ActionLink("Create New", "AAddUpdate")</p></i></button>
        </div>

    </div>
</div>

