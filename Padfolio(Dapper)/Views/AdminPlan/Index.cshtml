@model  IEnumerable<Padfolio_Dapper_.Models.Plans>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPageAdmin.cshtml";
}

<div class="card">
    <div class="card-body">
        <h5 class="card-title">Plans</h5>
        <!-- Table Variants -->
        <br />
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col"> &nbsp @Html.DisplayNameFor(model => model.PlanId)</th>
                    <th scope="col">@Html.DisplayNameFor(model => model.PlanTitle)</th>
                    <th scope="col">@Html.DisplayNameFor(model => model.PPrice)</th>
                    <th scope="col">@Html.DisplayNameFor(model => model.PExplanation)</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="table-primary">
                        <th scope="row">&nbsp &nbsp @Html.DisplayFor(modelItem => item.PlanId)</th>
                        <td>@Html.DisplayFor(modelItem => item.PlanTitle)</td>
                        <td>@Html.DisplayFor(modelItem => item.PPrice)</td>
                        <td>@Html.DisplayFor(modelItem => item.PExplanation)</td>
                        <td>
                            <button type="button" class="btn btn-light rounded-pill"> <i class="bi bi-bookmark-plus-fill"></i>&nbsp<p>&nbsp @Html.ActionLink("Edit", "PAddUpdate", new { id = item.PlanId }) &nbsp</p></button>&nbsp&nbsp
                            <button type="button" class="btn btn-light rounded-pill"> <i class="bi bi-bookmark-x-fill"></i>&nbsp<p>&nbsp @Html.ActionLink("Delete", "PDelete", new { id = item.PlanId })&nbsp</p></button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <!-- End Table Variants -->
        <br />
        <br />
        <div class="text-center">
            <button type="button" class="btn btn-light rounded-pill"><i class="bi bi-journal-plus"><p>@Html.ActionLink("Create New", "PAddUpdate")</p></i></button>
        </div>

    </div>
</div>

